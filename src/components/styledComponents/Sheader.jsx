import styled from 'styled-components';

const StyledHeader = styled.div`
  .header {
    background: #f4f4f4;
    text-align: center;
    padding: 2em;
    font-weight: bold;
  }

  .logo {
    max-width: 110px;
  }

  .title {
    font-size: 3em;
  }
`;

export default StyledHeader;
