import styled from 'styled-components';

const StyledMain = styled.div`
  .new-member-form {
    margin: 2em 0 4em 0;
    text-align: center;
  }

  label {
    display: block;
    margin-bottom: 0.5em;
  }

  input {
    margin-right: 0.5em;
  }

  h2 {
    text-align: center;
    font-weight: bold;
    font-size: 1.5em;
    padding-top: 1em;
  }

  .member-list {
    columns: 3;
  }

  .member {
    padding-top: 1em;
  }
`;

export default StyledMain;
